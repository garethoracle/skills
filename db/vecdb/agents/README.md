# Agent Metadata

This directory contains agent-specific metadata for the VecDB skill.

`openai.yaml` is UI metadata used by OpenAI/Codex skill surfaces. It should
stay aligned with `../SKILL.md` whenever the skill description or default user
prompt changes.
